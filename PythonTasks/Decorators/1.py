
import time

def decorate(func):
    def wrapper():
        start = time.time()
        end = time.time()
        print((end - start), 'Время выполнения: ', decorator)
        func()
    return wrapper

def func():
    for i in range(0, 1000,):
        return fun
        print(fun)
decorator = decorate(fun)
decorator()
